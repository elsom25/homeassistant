substitutions:
  name: home-assistant-voice-09a05c
  friendly_name: Home Assistant Voice — Kitchen
packages:
  Nabu Casa.Home Assistant Voice PE: github://esphome/home-assistant-voice-pe/home-assistant-voice.yaml
esphome:
  name: ${name}
  name_add_mac_suffix: false
  friendly_name: ${friendly_name}
api:
  encryption:
    key: N36fxAWrXqwhGKjFxVCl266rSduQljMFtKktg5FKwDY=

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

# === Voice PE overrides for noisy rooms ===
voice_assistant:
  # Make the mic less fooled by background music (0..4; 4 = strongest)
  noise_suppression_level: 4
  # Keep your voice dominant without lifting background too much
  auto_gain: 12dBFS
  # Lower the PE's own TTS so replies don't keep the mic "hot"
  volume_multiplier: 0.7